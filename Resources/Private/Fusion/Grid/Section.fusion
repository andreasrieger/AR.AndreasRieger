prototype(Site:Section) < prototype(Content) {
  templatePath = 'resource://AR.AndreasRieger/Private/Templates/NodeTypes/Section.html'
  attributes.class = 'container-fluid'
  titleOverride = ${q(node).property('titleOverride') ? q(node).property('titleOverride') : q(node).property('title')}
}
